<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data noupdate="0">
    <!-- Permission category -->
    <record model="ir.module.category" id="powerbi.report_category">
      <field name="name">Power BI</field>
      <field name="description">Power BI Access</field>
      <field name="sequence">10</field>
    </record>

 
    <!-- <record id="group_powerbi_user" model="res.groups">
      <field name="name">Power BI User</field>
      <field name="comment">Can access assigned reports</field>
      <field name="category_id" ref="powerbi.report_category"/>
    </record> -->

    <!-- Power BI Admin Group -->
    <!-- <record id="group_powerbi_admin" model="res.groups">
      <field name="name">Power BI Admin</field>
      <field name="comment">Full access to reports</field>
      <field name="category_id" ref="powerbi.report_category"/>
      <field name="implied_ids" eval="[(4, ref('group_powerbi_user'))]"/>
    </record> -->

    <!-- Admin Rule -->
    <!-- <record id="rule_powerbi_admin" model="ir.rule">
      <field name="name">Power BI Admin Access</field>
      <field name="model_id" ref="model_pbi_report"/>
      <field name="global" eval="False"/>
      <field name="groups" eval="[(4, ref('group_powerbi_admin'))]"/>
      <field name="domain_force">[(1, '=', 1)]</field>
    </record> -->

    <!-- User Rule -->
    <!-- <record id="rule_powerbi_user" model="ir.rule">
      <field name="name">Power BI User Access</field>
      <field name="model_id" ref="model_pbi_report"/>
      <field name="global" eval="False"/>
      <field name="groups" eval="[(4, ref('group_powerbi_user'))]"/>
      <field name="domain_force">[('access_users', 'in', user.id)]</field>
    </record> -->
  </data>
</odoo>
